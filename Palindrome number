class Solution {
    public boolean isPalindrome(int x) {
        if(x<0){
            return false;
        }
        int n=0;
        int a=x;
        while(a>0){
            int d=a%10;
            n=n*10+d;
            a=a/10;

        }
        return (x==n);
    }
}
